// Universal Fast I/O Template for Competitive Programming
#include <bits/stdc++.h> // Includes all standard libraries
using namespace std;

// Typedefs for faster coding
typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vll;
typedef pair<int, int> pii;

#define PB push_back
#define MP make_pair
#define F first
#define S second
#define FOR(i, a, b) for(int i = (a); i < (b); ++i)

void solve() {
    // Write your logic for a single test case here
    int n;
    cin >> n;
    
    // Instead of taking reading inputs natively, let's just log
    cout << "Solving test case for N = " << n << "\n";
}

int main() {
    // Fast I/O optimizations
    // Disable synchronization between C and C++ standard streams
    ios_base::sync_with_stdio(false);
    // Untie cin from cout
    cin.tie(NULL);
    
    // Example test case format
    /*
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    */
    
    cout << "Fast I/O template successfully executing...\n";
    cout << "Use this structure for TCS NQT platform.\n";
    
    return 0;
}
